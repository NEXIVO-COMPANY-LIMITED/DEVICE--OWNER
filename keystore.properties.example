# Copy to keystore.properties and fill in your values.
# Used by app/build.gradle.kts for release signing and by build_and_checksum.ps1.
# Do NOT commit keystore.properties or your .jks file.

storeFile=../your-release-keystore.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
