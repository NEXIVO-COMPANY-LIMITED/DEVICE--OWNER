<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Device information permissions -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    
    <!-- Camera permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    
    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Boot receiver permission -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Overlay permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Wake lock permission -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Foreground service permission (Android 9+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DEVICEOWNER"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ui.activities.setup.SetupActivity"
            android:exported="false" />
        
        <!-- Device Owner Installation Status Activity -->
        <!-- Automatically shown after Device Owner initialization completes -->
        <activity
            android:name=".ui.activities.DeviceOwnerInstallationStatusActivity"
            android:exported="false"
            android:theme="@style/Theme.DEVICEOWNER"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:excludeFromRecents="false" />
        
        <!-- Provisioning Mode Activity -->
        <!-- Required for Android 12+ QR code provisioning -->
        <activity
            android:name=".ui.provisioning.ProvisioningModeActivity"
            android:exported="true"
            android:theme="@style/Theme.DEVICEOWNER">
            <intent-filter>
                <action android:name="android.app.action.GET_PROVISIONING_MODE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
        <!-- Policy Compliance Activity -->
        <!-- Required for Android 12+ QR code provisioning -->
        <activity
            android:name=".ui.provisioning.PolicyComplianceActivity"
            android:exported="true"
            android:theme="@style/Theme.DEVICEOWNER">
            <intent-filter>
                <action android:name="android.app.action.ADMIN_POLICY_COMPLIANCE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
        <!-- Device Admin Receiver -->
        <!-- Required for device owner provisioning via QR code, NFC, or ADB -->
        <receiver
            android:name=".receivers.AdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLE_REQUESTED" />
                <!-- Required for QR code and NFC provisioning -->
                <action android:name="android.app.action.PROVISION_MANAGED_DEVICE" />
                <action android:name="android.app.action.PROVISION_MANAGED_DEVICE_FROM_TRUSTED_SOURCE" />
            </intent-filter>
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
        </receiver>
        
        <!-- Services -->
        <service
            android:name=".services.UnifiedHeartbeatService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <service
            android:name=".services.HeartbeatVerificationService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <service
            android:name=".services.CommandQueueService"
            android:exported="false" />
        
        <service
            android:name=".services.TamperDetectionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <service
            android:name=".services.FlashingDetectionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <service
            android:name=".services.ComprehensiveSecurityService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <service
            android:name=".services.DeviceOwnerRecoveryService"
            android:exported="false" />
        
        <service
            android:name=".services.MismatchAlertRetryService"
            android:exported="false" />
        
        <service
            android:name=".overlay.OverlayManager"
            android:exported="false" />
        <!-- Receivers -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        
        <receiver
            android:name=".receivers.PackageRemovalReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <action android:name="android.intent.action.PACKAGE_FULLY_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>
        
        <receiver
            android:name=".receivers.RebootDetectionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>